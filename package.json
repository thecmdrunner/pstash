{
  "name": "pstash",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "build-lambda-pkg": "rm my-lambda-package.zip ; zip -r my-lambda-package.zip index.js src ffmpeg ffprobe node_modules package.json package-lock.json",
    "build": "rm dist -rf && mkdir dist && tsc",
    "deploy": "npm run build && serverless deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@axiomhq/js": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-plugin-typescript": "^2.1.5"
  }
}
